<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
<uses-permission android:name="android.permission.VIBRATE"/>
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />





    <application
        android:allowBackup="true"
        android:label="Jalkster"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Jalkster">

        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.Jalkster.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

<activity android:name=".JalkLiveSessionActivity" android:exported="false" android:launchMode="singleTop" android:showWhenLocked="true" android:turnScreenOn="true">
    <!-- If this activity can be launched directly by the app’s UI, keep its intent filter -->
    <!-- Example: -->
    <!--
    <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
    </intent-filter>
    -->
</activity>

        <activity
            android:name=".JalkSessionConfigActivity"
            android:exported="false" />

        <activity
            android:name=".XCSkiSessionConfigActivity"
            android:exported="false" />

        <activity
            android:name=".XCSkiLiveSessionActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

        <activity
            android:name=".PastSessionDetailActivity"
            android:exported="false" />

        <activity
            android:name=".PastSessionEditActivity"
            android:exported="false" />

        <activity
            android:name=".PastSessionsActivity"
            android:exported="false" />

        <activity
            android:name=".StatsActivity"
            android:exported="false" />

        <activity
            android:name=".JalkMainActivity"
            android:exported="false" />

<service
    android:name=".JalkTimerService"
    android:exported="false"
    android:foregroundServiceType="dataSync"
    android:permission="android.permission.FOREGROUND_SERVICE" />

        <receiver
            android:name=".TimerExpiredReceiver"
            android:exported="false" />


    </application>

</manifest>
